<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proiectdam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProiectDAM">
        <activity android:name=".IntrebariActivity"></activity>
        <activity android:name=".ChestionarActivity" />
        <activity android:name=".UnitatiActivity" />
        <activity android:name=".TestareFullImageActivity" />
        <activity android:name=".TestareActivity" />
        <activity android:name=".InformatiiActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>